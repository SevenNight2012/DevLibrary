<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:background="@drawable/white_round_bg">

    <ProgressBar
        android:id="@+id/dialog_loading"
        android:layout_width="@dimen/app_64_dp"
        android:layout_height="@dimen/app_64_dp"
        android:layout_centerHorizontal="true"/>

    <TextView
        android:id="@+id/dialog_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_below="@+id/dialog_loading"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="@string/dialog_loading"
        android:textColor="@color/color_333333"
        android:textSize="@dimen/app_16_sp"/>

</RelativeLayout>